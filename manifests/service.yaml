apiVersion: v1
kind: Service
metadata:
  name: hashresponse-svc
spec:
  type: NodePort
  selector:
    app: hashresponse
  ports:
    - name: http
      port: 1234
      nodePort: 30080
      targetPort: 3000
      protocol: TCP
